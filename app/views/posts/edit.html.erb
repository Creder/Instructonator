<h1>Editing Post</h1>

<%= render 'form', post: @post %>
<div id = "steps">
  <% if @post.steps.any? %>
  <%= render @post.steps =%>
  <% else %>
  <p> Not found any step</p>
  <% end %>
</div>
  
<%= link_to 'Add Step', new_post_step_path(@post) %>
<%= link_to 'Show', @post %> |
<%= link_to 'Back', posts_path %>

<%= javascript_include_tag "posts", "data-turbolinks-track" => "reload" %>
