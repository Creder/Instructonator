%p#notice= notice
%h1 Posts
%table
  %thead
    %tr
      %th Title
      %th{:colspan => "3"}
  %tbody
    - @posts.each do |post|
      %tr
        %td
          = link_to post.title, post
          Tags : #{raw post.tag_list.map {|t| link_to t, tag_path(t) }.join(', ')}
%br/
- if signed_in?
  = link_to 'New Post', new_post_path
- else
  %p You must sign in for create posts!